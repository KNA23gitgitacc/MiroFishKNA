# ===========================================
# MiroFish Backend Dependencies (Local-Only)
# ===========================================
# Python 3.11+ required
# Install: pip install -r requirements.txt
# ===========================================

# ============= Core Framework =============
flask>=3.0.0
flask-cors>=6.0.0

# ============= LLM =============
# OpenAI SDK (unified OpenAI format — works with Ollama, OpenAI, etc.)
openai>=1.0.0

# HTTP client (for Ollama embedding API)
requests>=2.28.0

# ============= Graph Database =============
# Neo4j Community Edition (replaces Zep Cloud)
neo4j>=5.15.0

# ============= OASIS Social Simulation =============
camel-oasis==0.2.5
camel-ai==0.2.78

# ============= File Processing =============
PyMuPDF>=1.24.0
# Encoding detection (supports non-UTF-8 text files)
charset-normalizer>=3.0.0
chardet>=5.0.0

# ============= Utilities =============
# Environment variables
python-dotenv>=1.0.0

# Data validation
pydantic>=2.0.0
